define([
  'document/annotation/image/selection/layers/style'
], function(Style) {

  var ToponymDrawingTool = function(olMap) {

    var createNewSelection = function(e) {

        };

    this.createNewSelection = createNewSelection;

  };

  return ToponymDrawingTool;

});
